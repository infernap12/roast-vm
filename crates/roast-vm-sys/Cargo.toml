[package]
name = "roast-vm-sys"
version = "0.1.0"
edition = "2024"
publish = ["nexus"]

[dependencies]
jni = { workspace = true }
roast-vm-core = { workspace = true }
log = "0.4.28"

[lib]
name = "roast_vm"
crate-type = ["cdylib"]